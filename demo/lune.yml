name: Demo
app_entry: src/main.cr
icon: ../assets/lune-logo.png
frontend:
  dir: frontend
  install: npm install
  build: npm run build
  dev:
    cmd: npm run dev
    url: http://localhost:5173
mac:
  sign: "Developer ID Application: Your Name (TEAMID)" # enables UNUserNotificationCenter
  notarize: false

url_schemes:
  - lune-demo

capabilities:
  # include:
  # - system

  # exclude:
  #   - system
